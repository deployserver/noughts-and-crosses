define(function() {

   /**
    *
    * @param {String} name
    * @param {Number} index
    * @constructor
    */
   function User(name, index) {
      this.name = name;
      this.index = index;
   }

   return User;

});
